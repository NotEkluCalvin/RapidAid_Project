specVersion: "v1beta"
serviceId: "rapidaid"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "rapidaid-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
